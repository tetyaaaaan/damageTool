# Genshin r2 weapon primary-field search — shard 04

This is a draft-only, candidate-scoped external-evidence frontier. It reuses
the persisted 6.7 and 7.0 `genshin-db` raw records and performs no network
fetch, semantic inference, certificate issuance, canonical promotion, or
authoritative queue mutation.

- Scope: the nine remaining safe numeric `weaponEffectSpec` candidates after
  registered Shards 01–03, legacy decisions, and the separate
  `w_12516_stat_1` historical-canonical route. There are seven weapon entities.
- Primary observation: 9/9 raw records have valid bytes, SHA-256 and Git blob
  pins, and 9/9 6.7→7.0 refinement vectors agree numerically. This remains an
  observation only; it does not establish semantic identity of a value column.
- Independent provider field bundles: 0/9 candidates and 0/36 required
  candidate×claim fields. Official HoYoWiki has no candidate field record in
  the persisted bounded materialization; the finite gcsim ledger has no
  intersection with this tail.
- Frontier: candidate-scoped `searchExhausted` with explicit reopen triggers
  for an immutable provider manifest, exact raw/field digests, explicit 7.0
  binding, independent lineage, and comparable candidate×claim semantics.
- Disposition: 9/9 `evidenceDeferred`; strict verification, eligibility
  certificate, semantic mapping, Runtime and canonical promotion remain
  false/zero. The current queue remains `searchRequired` and is intentionally
  unchanged by this draft.

Artifact field digest: `624492cc0bff665016b28d5fca5f3379be95f9858b21e831383512112ef8db27`

Artifact SHA-256: generated after the final artifact write; verify from the
path recorded by the parent checkpoint before any future registry integration.
